# API Configuration
# Backend API Base URL (used in axios baseURL config)
# Leave as "/" to use the same origin (works with proxies)
# Or set to "http://localhost:3000" for direct API calls during development

VITE_API_URL=/
